# 🏗️ System Architecture Diagram

## Overall System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                                 │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                     React Application                          │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │  │
│  │  │   Auth UI   │  │  Quran UI   │  │    Streaming UI     │   │  │
│  │  │  (Sign In)  │  │  (Reader)   │  │  (Start/Watch)      │   │  │
│  │  └──────┬──────┘  └─────────────┘  └──────┬──────────────┘   │  │
│  │         │                                   │                   │  │
│  │  ┌──────▼────────────────────────────────┐ │                   │  │
│  │  │       AuthContext (State Mgmt)        │ │                   │  │
│  │  │  - Current User                        │ │                   │  │
│  │  │  - User Role (user/scholar/imam)      │ │                   │  │
│  │  │  - Session Token                       │ │                   │  │
│  │  └───────────────────────────────────────┘ │                   │  │
│  │                                              │                   │  │
│  │  ┌───────────────────────────────────────┐ │                   │  │
│  │  │     ProtectedRoute Component          │ │                   │  │
│  │  │  - Check user role                     │ │                   │  │
│  │  │  - Allow/deny access                   │ │                   │  │
│  │  │  - Redirect if unauthorized            │ │                   │  │
│  │  └───────────────────────────────────────┘ │                   │  │
│  │                                              │                   │  │
│  └──────────────────────────────────────────────┼───────────────────┘  │
│                                                  │                       │
│         ┌────────────────────────────────────────┘                      │
│         │                                                                │
│         ▼                                                                │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │              Agora RTC SDK (agora.ts)                         │      │
│  │  - AgoraService class                                          │      │
│  │  - createClient()                                              │      │
│  │  - joinChannel() ← Calls generateAgoraToken()                 │      │
│  │  - publishTracks()                                             │      │
│  │  - subscribeToUser()                                           │      │
│  └────────────────────┬───────────────────────────────────────────┘      │
│                       │                                                   │
└───────────────────────┼───────────────────────────────────────────────────┘
                        │
            ┌───────────┴──────────┐
            │                      │
            ▼                      ▼
┌───────────────────────┐  ┌────────────────────────────────┐
│   Supabase Backend    │  │       Agora RTC Network         │
│                       │  │                                 │
│  ┌─────────────────┐  │  │  ┌──────────────────────────┐  │
│  │  Authentication │  │  │  │   Video/Audio Streaming  │  │
│  │  - JWT Tokens   │  │  │  │   - Real-time WebRTC     │  │
│  │  - Session Mgmt │  │  │  │   - Low Latency          │  │
│  └─────────────────┘  │  │  └──────────────────────────┘  │
│                       │  │                                 │
│  ┌─────────────────┐  │  │  ┌──────────────────────────┐  │
│  │   PostgreSQL    │  │  │  │   Token Validation       │  │
│  │   Database      │  │  │  │   - Checks token         │  │
│  │   - profiles    │  │  │  │   - Grants access        │  │
│  │   - streams     │  │  │  │   - Role enforcement     │  │
│  │   - consults    │  │  │  └──────────────────────────┘  │
│  │   - donations   │  │  │                                 │
│  └─────────────────┘  │  └─────────────────────────────────┘
│                       │           ▲
│  ┌─────────────────┐  │           │
│  │  Edge Function  │  │           │
│  │  generate-token │◄─┼───────────┘
│  │                 │  │    Token validates here
│  │  1. Auth check  │  │
│  │  2. Role check  │  │
│  │  3. Build token │  │
│  │  4. Return JWT  │  │
│  └─────────────────┘  │
│                       │
└───────────────────────┘
```

---

## Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Enter email/password
     ▼
┌────────────────┐
│  UserSignIn.tsx│
│  Component     │
└────┬───────────┘
     │
     │ 2. Call signIn()
     ▼
┌────────────────┐
│  AuthContext   │
│  signIn()      │
└────┬───────────┘
     │
     │ 3. supabase.auth.signInWithPassword()
     ▼
┌─────────────────────┐
│  Supabase Auth API  │
│  - Verify password  │
│  - Generate JWT     │
│  - Return session   │
└────┬────────────────┘
     │
     │ 4. Fetch user profile from database
     ▼
┌─────────────────────┐
│  profiles table     │
│  SELECT * WHERE ... │
│  Returns: role, ... │
└────┬────────────────┘
     │
     │ 5. Store in context
     ▼
┌─────────────────────┐
│  AuthContext State  │
│  - session          │
│  - user             │
│  - profile (role)   │
└────┬────────────────┘
     │
     │ 6. Redirect based on role
     ▼
┌─────────────────────┐
│  Dashboard/Home     │
└─────────────────────┘
```

---

## Streaming Flow (Scholar)

```
┌──────────────┐
│   Scholar    │
│   clicks     │
│ "Start Stream"│
└──────┬───────┘
       │
       │ 1. Call startStream()
       ▼
┌────────────────────────┐
│ ScholarLiveStream.tsx  │
│                        │
│ const startStream = () │
└──────┬─────────────────┘
       │
       │ 2. agoraService.joinChannel(channel, uid, 'host')
       ▼
┌────────────────────────┐
│  agora.ts              │
│  joinChannel()         │
└──────┬─────────────────┘
       │
       │ 3. generateAgoraToken(channel, uid, 'host')
       ▼
┌──────────────────────────────────────┐
│  generateAgoraToken()                │
│  - Get session token from Supabase   │
│  - Call Edge Function                │
└──────┬───────────────────────────────┘
       │
       │ 4. POST /functions/v1/generate-agora-token
       │    Headers: Authorization: Bearer <jwt>
       │    Body: { channelName, uid, role: 'host' }
       ▼
┌────────────────────────────────────────────┐
│  Supabase Edge Function                    │
│  supabase/functions/generate-agora-token/  │
│                                            │
│  1. Verify JWT token                       │
│  2. Get user ID from token                 │
│  3. Query profiles table for role          │
│  4. Check role is 'scholar' or 'imam'      │
│  5. Build Agora RTC token using:           │
│     - AGORA_APP_ID                         │
│     - AGORA_APP_CERTIFICATE (secret)       │
│     - channelName                          │
│     - uid                                  │
│     - role: PUBLISHER (1)                  │
│     - expiration: 3600 seconds             │
│  6. Return { token, uid, expiresAt }       │
└──────┬─────────────────────────────────────┘
       │
       │ 5. Return token to frontend
       ▼
┌────────────────────────┐
│  agora.ts              │
│  - Receives token      │
│  - Joins channel       │
└──────┬─────────────────┘
       │
       │ 6. client.join(appId, channel, token, uid)
       ▼
┌───────────────────────────────────┐
│  Agora RTC Network                │
│  - Validates token signature      │
│  - Checks expiration              │
│  - Verifies role (publisher)      │
│  - Grants access ✅               │
└──────┬────────────────────────────┘
       │
       │ 7. Create local tracks (camera + mic)
       ▼
┌────────────────────────┐
│  AgoraService          │
│  createLocalTracks()   │
│  - Video track         │
│  - Audio track         │
└──────┬─────────────────┘
       │
       │ 8. Publish tracks to channel
       ▼
┌────────────────────────┐
│  Agora RTC Network     │
│  - Broadcast to all    │
│    subscribers         │
└────────────────────────┘
       │
       │ 9. Insert stream record
       ▼
┌────────────────────────┐
│  Supabase DB           │
│  INSERT INTO streams   │
│  (scholar_id, channel) │
└────────────────────────┘

     🎥 Stream is LIVE!
```

---

## Viewing Flow (Member)

```
┌──────────────┐
│   Member     │
│   clicks     │
│ "Join Stream"│
└──────┬───────┘
       │
       │ 1. Call joinStream(channelName)
       ▼
┌─────────────────────────────┐
│ UserPrayerServiceViewer.tsx │
│                             │
│ const joinStream = () ...   │
└──────┬──────────────────────┘
       │
       │ 2. agoraService.joinChannel(channel, uid, 'audience')
       ▼
┌────────────────────────┐
│  agora.ts              │
│  joinChannel()         │
└──────┬─────────────────┘
       │
       │ 3. generateAgoraToken(channel, uid, 'audience')
       ▼
┌──────────────────────────────────────┐
│  generateAgoraToken()                │
│  - Get session token from Supabase   │
│  - Call Edge Function                │
└──────┬───────────────────────────────┘
       │
       │ 4. POST /functions/v1/generate-agora-token
       │    Headers: Authorization: Bearer <jwt>
       │    Body: { channelName, uid, role: 'audience' }
       ▼
┌────────────────────────────────────────────┐
│  Supabase Edge Function                    │
│                                            │
│  1. Verify JWT token                       │
│  2. Get user ID from token                 │
│  3. Query profiles table (any role OK)     │
│  4. Build Agora RTC token with:            │
│     - role: SUBSCRIBER (2)                 │
│  5. Return { token, uid, expiresAt }       │
└──────┬─────────────────────────────────────┘
       │
       │ 5. Return token to frontend
       ▼
┌────────────────────────┐
│  agora.ts              │
│  - Receives token      │
│  - Joins as subscriber │
└──────┬─────────────────┘
       │
       │ 6. client.join(appId, channel, token, uid)
       ▼
┌───────────────────────────────────┐
│  Agora RTC Network                │
│  - Validates token signature      │
│  - Checks expiration              │
│  - Verifies role (subscriber)     │
│  - Grants VIEW-ONLY access ✅     │
└──────┬────────────────────────────┘
       │
       │ 7. Listen for remote users
       ▼
┌────────────────────────┐
│  AgoraService          │
│  client.on(            │
│    'user-published'    │
│  )                     │
└──────┬─────────────────┘
       │
       │ 8. Subscribe to remote tracks
       ▼
┌────────────────────────┐
│  subscribeToUser()     │
│  - Subscribe video     │
│  - Subscribe audio     │
│  - Play tracks         │
└────────────────────────┘

     👀 Watching Stream!
```

---

## Database Schema

```
┌─────────────────────────────────────────┐
│            profiles                      │
├─────────────────────────────────────────┤
│ id (uuid)             PK                │
│ email (text)          UNIQUE            │
│ full_name (text)                        │
│ role (text)           DEFAULT 'user'    │
│   - 'user'                              │
│   - 'scholar'                           │
│   - 'imam'                              │
│   - 'admin'                             │
│ created_at (timestamp)                  │
│ updated_at (timestamp)                  │
└─────────────────────────────────────────┘
              │
              │ FK: scholar_id
              ▼
┌─────────────────────────────────────────┐
│            streams                       │
├─────────────────────────────────────────┤
│ id (uuid)             PK                │
│ scholar_id (uuid)     FK → profiles.id  │
│ title (text)                            │
│ channel_name (text)   UNIQUE            │
│ is_active (boolean)   DEFAULT true      │
│ started_at (timestamp)                  │
│ ended_at (timestamp)                    │
│ created_at (timestamp)                  │
└─────────────────────────────────────────┘
              │
              │ FK: scholar_id
              ▼
┌─────────────────────────────────────────┐
│          consultations                   │
├─────────────────────────────────────────┤
│ id (uuid)             PK                │
│ user_id (uuid)        FK → profiles.id  │
│ scholar_id (uuid)     FK → profiles.id  │
│ title (text)                            │
│ description (text)                      │
│ preferred_date (date)                   │
│ preferred_time (time)                   │
│ status (text)         DEFAULT 'pending' │
│   - 'pending'                           │
│   - 'confirmed'                         │
│   - 'completed'                         │
│   - 'cancelled'                         │
│ created_at (timestamp)                  │
└─────────────────────────────────────────┘
              │
              │ FK: scholar_id, user_id
              ▼
┌─────────────────────────────────────────┐
│            donations                     │
├─────────────────────────────────────────┤
│ id (uuid)             PK                │
│ user_id (uuid)        FK → profiles.id  │
│ scholar_id (uuid)     FK → profiles.id  │
│ amount (numeric)                        │
│ currency (text)       DEFAULT 'USD'     │
│ message (text)                          │
│ created_at (timestamp)                  │
└─────────────────────────────────────────┘
```

---

## Row-Level Security (RLS) Policies

```
┌────────────────────────────────────────────────────────┐
│  profiles                                               │
├────────────────────────────────────────────────────────┤
│  SELECT:  Anyone can read all profiles                 │
│  INSERT:  Authenticated users (on signup)              │
│  UPDATE:  Users can update their own profile only      │
│  DELETE:  Only admins (custom policy)                  │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  streams                                                │
├────────────────────────────────────────────────────────┤
│  SELECT:  Anyone can read all streams                  │
│  INSERT:  Only scholars/imams can create streams       │
│  UPDATE:  Only stream owner can update                 │
│  DELETE:  Only stream owner can delete                 │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  consultations                                          │
├────────────────────────────────────────────────────────┤
│  SELECT:  Users see own bookings, scholars see theirs  │
│  INSERT:  Only users (not scholars) can book           │
│  UPDATE:  Scholar can update status                    │
│  DELETE:  User can cancel own booking                  │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  donations                                              │
├────────────────────────────────────────────────────────┤
│  SELECT:  Donor sees own, scholar sees received        │
│  INSERT:  Only users (not scholars) can donate         │
│  UPDATE:  No one                                       │
│  DELETE:  No one (keep records)                        │
└────────────────────────────────────────────────────────┘
```

---

## Component Hierarchy

```
App.tsx (Router)
│
├─ AuthProvider (Context)
│   │
│   ├─ Public Routes
│   │   ├─ UserSignIn
│   │   └─ UserSignUp
│   │
│   ├─ Protected Routes (All Users)
│   │   ├─ PrayerTimes
│   │   ├─ QuranReader
│   │   └─ QiblaDirection
│   │
│   ├─ Protected Routes (Members Only)
│   │   ├─ UserPrayerServiceViewer (Watch Streams)
│   │   ├─ ZakatDonation
│   │   └─ ConsultationBooking
│   │
│   └─ Protected Routes (Scholars/Imams Only)
│       ├─ ScholarLiveStream (Start Stream)
│       └─ ScholarConsultationManager
│
└─ UI Components (Shared)
    ├─ Button
    ├─ Card
    └─ Input
```

---

## Permission Matrix Logic

```typescript
// In ProtectedRoute.tsx

export const usePermissions = () => {
  const { profile } = useAuth()
  
  return {
    // Scholars and Imams can stream
    canStream: ['scholar', 'imam', 'admin'].includes(profile?.role),
    
    // Only regular users can donate (not scholars)
    canDonate: profile?.role === 'user',
    
    // Only regular users can book consultations
    canBookConsultation: profile?.role === 'user',
    
    // Scholars and Imams can manage consultations
    canManageConsultations: ['scholar', 'imam', 'admin'].includes(profile?.role),
    
    // Everyone can read Quran, see prayer times, use Qibla
    canAccessIslamicFeatures: true
  }
}
```

---

## Environment Variables Flow

```
┌───────────────────────────┐
│  .env (Local Development) │
├───────────────────────────┤
│  VITE_SUPABASE_URL        │ ─────┐
│  VITE_SUPABASE_ANON_KEY   │ ─────┤
│  VITE_AGORA_APP_ID        │ ─────┤
└───────────────────────────┘      │
                                   │
┌───────────────────────────────┐  │
│  Vercel/Netlify (Production)  │  │
├───────────────────────────────┤  │
│  VITE_SUPABASE_URL            │ ─┤
│  VITE_SUPABASE_ANON_KEY       │ ─┤
│  VITE_AGORA_APP_ID            │ ─┤
└───────────────────────────────┘  │
                                   │
                                   ▼
                        ┌────────────────────┐
                        │  React App Build   │
                        │  (Compiled in)     │
                        └────────────────────┘

┌─────────────────────────────────┐
│  Supabase Edge Function Secrets │
│  (Server-side only)              │
├─────────────────────────────────┤
│  AGORA_APP_ID                   │ ─────┐
│  AGORA_APP_CERTIFICATE          │ ─────┤
└─────────────────────────────────┘      │
         (Set via CLI)                   │
                                         ▼
                              ┌────────────────────────┐
                              │  Edge Function Runtime │
                              │  (Never exposed)       │
                              └────────────────────────┘
```

---

## Security Boundaries

```
┌────────────────────────────────────────────────────────────┐
│                     PUBLIC (Browser)                        │
│  ✅ Can See:                                               │
│     - Supabase URL                                         │
│     - Supabase Anon Key                                    │
│     - Agora App ID                                         │
│                                                            │
│  ❌ Cannot See:                                            │
│     - Agora App Certificate (server-only)                 │
│     - Database passwords (managed by Supabase)            │
│     - Other users' session tokens                         │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                  PROTECTED (Edge Function)                  │
│  ✅ Has Access To:                                         │
│     - Agora App Certificate (build tokens)                │
│     - User authentication status                          │
│     - User roles from database                            │
│                                                            │
│  🔒 Security Measures:                                     │
│     - Validates JWT token from Supabase                   │
│     - Checks user role before generating token            │
│     - Rate limiting (500k invocations/month free)         │
│     - Logging for audit trail                             │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                   PROTECTED (Agora Network)                 │
│  ✅ Enforces:                                              │
│     - Token signature validation                           │
│     - Token expiration (1 hour)                           │
│     - Role-based permissions (publisher/subscriber)       │
│     - Channel access control                              │
│                                                            │
│  🔒 Prevents:                                              │
│     - Unauthorized streaming                              │
│     - Token forgery                                       │
│     - Expired token usage                                 │
│     - Role escalation (viewer → publisher)                │
└────────────────────────────────────────────────────────────┘
```

---

## Deployment Pipeline

```
┌──────────────┐
│ Local Dev    │
│              │
│ npm run dev  │
└──────┬───────┘
       │
       │ 1. Code & test locally
       ▼
┌──────────────────┐
│ Git Repository   │
│                  │
│ git push origin  │
└──────┬───────────┘
       │
       │ 2. Push to GitHub/GitLab
       ▼
┌─────────────────────────────┐
│ Vercel/Netlify              │
│                             │
│ 1. Detect git push          │
│ 2. Run: npm run build       │
│ 3. Deploy dist/ folder      │
│ 4. Set env vars             │
└──────┬──────────────────────┘
       │
       │ 3. Build & deploy frontend
       ▼
┌─────────────────────────────┐
│ Production URL              │
│ https://yourapp.vercel.app  │
└─────────────────────────────┘

┌──────────────────────────────┐
│ Supabase Dashboard           │
│                              │
│ 1. Run SQL (create tables)   │
│ 2. Deploy Edge Function      │
│    supabase functions deploy │
└──────┬───────────────────────┘
       │
       │ 4. Backend ready
       ▼
┌─────────────────────────────┐
│ Supabase Production         │
│ - Database                  │
│ - Auth                      │
│ - Edge Functions            │
└─────────────────────────────┘
```

---

This architecture provides:
- ✅ **Security**: Tokens generated server-side only
- ✅ **Scalability**: Serverless edge functions
- ✅ **Performance**: WebRTC for low-latency streaming
- ✅ **Maintainability**: Clear separation of concerns
- ✅ **Cost-Effective**: Pay-as-you-go pricing

