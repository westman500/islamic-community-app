# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Supabase
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Agora (for live streaming)
VITE_AGORA_APP_ID=your_agora_app_id

# ⚠️ SECURITY NOTE: Certificate is NO LONGER stored in frontend
# The AGORA_APP_CERTIFICATE is now stored server-side in Supabase Edge Function
# 
# To set Supabase Edge Function secrets:
# 1. Install Supabase CLI: npm install -g supabase
# 2. Login: supabase login
# 3. Link project: supabase link --project-ref your-project-ref
# 4. Set secrets:
#    supabase secrets set AGORA_APP_ID=your-agora-app-id
#    supabase secrets set AGORA_APP_CERTIFICATE=your-agora-certificate
# 5. Deploy function: supabase functions deploy generate-agora-token
#
# See AGORA_TOKEN_DEPLOYMENT.md for full deployment guide

# Paystack (Payment Processing)
# Get these from: https://dashboard.paystack.com/#/settings/developers
VITE_PAYSTACK_PUBLIC_KEY=pk_test_your_public_key_here
VITE_PAYSTACK_SECRET_KEY=sk_test_your_secret_key_here

# Server-side secrets (set via Supabase CLI):
# supabase secrets set PAYSTACK_SECRET_KEY=sk_test_your_secret_key_here
#
# Note: For production, replace 'test' keys with 'live' keys after verification
# See PAYSTACK_WEBHOOK_SETUP.md for complete setup guide
